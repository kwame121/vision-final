# Third-party deps inferred from imports in src/ and scripts/*.py:
#   torch, torchvision (transforms, utils), tqdm, PIL (Pillow).
# GPU: install matching torch/torchvision from https://pytorch.org/get-started/locally/
#      then: pip install tqdm Pillow
#      or install this file and let pip resolve CUDA wheels if available for your platform.

torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0
tqdm>=4.65.0
Pillow>=9.0.0
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
httpx>=0.27.0,<1.0.0
python-dotenv>=1.0.0,<2.0.0
pydantic>=2.0.0,<3.0.0
